<?php require_once "common.lib"; ?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= (isset($page_title) ? $page_title . ' — ' : '') . $system_name ?></title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <?php for ($i = 0; $i < sizeof($css_file); $i++): ?>
        <link rel="stylesheet" href="<?= $css_base . $css_file[$i] ?>?v=<?= $version ?>">
    <?php endfor; ?>
    <?php for ($i = 0; $i < sizeof($js_file); $i++): ?>
        <script src="<?= $js_base . $js_file[$i] ?>?v=<?= $version ?>"></script>
    <?php endfor; ?>
    <input type="hidden" id="js_base"  value="<?= $js_base ?>">
    <input type="hidden" id="css_base" value="<?= $css_base ?>">
    <input type="hidden" id="img_base" value="<?= $img_base ?>">
</head>
<body class="login-body">
<div class="login-card">
    <div class="login-logo">
        <div class="login-logo-icon"><i class="fas fa-layer-group"></i></div>
        <h2><?= htmlspecialchars($system_name) ?></h2>
        <p>Sign in to your account</p>
    </div>
